services:
  - type: web
    name: myx-shop-stocks
    env: python
    plan: free
    buildCommand: pip install -r js/requirements.txt
    startCommand: cd js && gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
